{
  "name": "imap-drive-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only IMAP collector that forwards a tightly filtered dossier to an authenticated n8n upload workflow.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "check": "node --check src/index.mjs && node --check src/lib.mjs && node --check src/healthcheck.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "imapflow": "2.0.2",
    "mailparser": "3.9.25"
  }
}
